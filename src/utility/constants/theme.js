import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#60b0f4",
    },
    secondary: {
      main: "#fff",
    },
  },
});

export default theme;
